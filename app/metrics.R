metrics <- tabPanel("Metrics",
                    fluidRow(column(width = 1),
                             column(width = 10,
                                    h4("(Please wait a few seconds for the figures to load)"),
                                    hr()
                             ),
                             column(width = 1)
                    )
)
